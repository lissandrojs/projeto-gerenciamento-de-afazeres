import React from 'react'
import { InputContainer, InputText } from './styled'

const Input = ({...rest}) => {
  return (
    <InputContainer >
        <InputText />
    </InputContainer>
  )
}

export {Input}